{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
  {% for post in posts %}
    <div class="post-preview">
      <a href="{{ path('blog_post', {id: post.id, slug: post.slug}) }}">
        <h2 class="post-title">
          {{ post.title }}
        </h2>
        <h3 class="post-subtitle">
          {{ post.subtitle }}
        </h3>
      </a>
      <p class="post-meta">Posted on the {{ post.createdAt|date('jS F, Y') }}</p>
    </div>
    <hr>
  {% endfor %}
{% endblock %}
